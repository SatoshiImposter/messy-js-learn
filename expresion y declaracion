expressions. = cada vez que el codigo hace algo y se obtiene un valor es una expresion pero si no posee nada es una declaracion
declarations = cuando creo una variable que se le va adar valor despues
statements. = si no se puede poner en consola es un statements